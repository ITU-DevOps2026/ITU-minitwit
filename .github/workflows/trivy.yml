
name: Trivy Security Scan

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  trivy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install Trivy
        run: |
          sudo apt-get update
          sudo apt-get install -y wget gnupg lsb-release

          wget --max-redirect=0 --https-only \
            https://aquasecurity.github.io/trivy-repo/deb/public.key \
            -O trivy.key

          gpg --dearmor trivy.key
          sudo mv trivy.key.gpg /usr/share/keyrings/trivy.gpg

          echo "deb [signed-by=/usr/share/keyrings/trivy.gpg] https://aquasecurity.github.io/trivy-repo/deb $(lsb_release -sc) main" \
            | sudo tee /etc/apt/sources.list.d/trivy.list

          sudo apt-get update
          sudo apt-get install -y trivy

      
      - name: Scan Dockerfiles
        run: |
          trivy config . \
            --file-patterns "dockerfile:Dockerfile*" \
            --skip-files Dockerfile-mysql \
            --severity HIGH,CRITICAL \
            --exit-code 1

      - name: Build and scan all Docker images
        run: |
          shopt -s nullglob

          # Root Dockerfile-* files
          for file in Dockerfile-*; do
            echo "Found root file $file"

            name=$(echo "$file" | sed 's/Dockerfile-//')
            image="minitwit-$name"

            echo "Building $file → $image"
            docker build -f "$file" -t "$image:latest" .

            echo "Scanning $image"
            trivy image "$image:latest" \
              --severity HIGH,CRITICAL \
              --exit-code 1 \
              --no-progress
          done

          # Subfolder Dockerfiles
          for file in */Dockerfile; do
            echo "Found nested file $file"

            dir=$(dirname "$file")
            name=$(basename "$dir")
            image="minitwit-$name"

            echo "Building $file → $image"
            docker build -f "$file" -t "$image:latest" "$dir"

            echo "Scanning $image"
            trivy image "$image:latest" \
              --severity HIGH,CRITICAL \
              --exit-code 1 \
              --no-progress
          done
